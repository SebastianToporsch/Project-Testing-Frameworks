html(lang="en")
  head
    meta(charset="UTF-8")
    meta(http-equiv="X-UA-Compatible", content="IE=edge")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title= title
  body
  a.btn(href="/users") Press to see all users
  h1="Id / Username / Age / Email"
    ul
    for item in message
      li= "ID: " + item.id +" /  Username: "+ item.username +" / Age: " + item.age +" / Email: " +item.email